apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: monitor
  namespace: monitoring
  annotations:
    external-dns.alpha.kubernetes.io/hostname: monitor.sdpaks.equinor.com.
spec:
  tls:
  - hosts:
    - monitor.sdpaks.equinor.com
  rules:
    - host: monitor.sdpaks.equinor.com
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: monitor-auth
  namespace: monitoring
spec:
  tls:
  - hosts:
    - monitor.sdpaks.equinor.com
  rules:
    - host: monitor.sdpaks.equinor.com
