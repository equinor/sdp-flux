apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
    external-dns.alpha.kubernetes.io/hostname: test3.sdp.handlel.app.
    nginx.ingress.kubernetes.io/auth-url: "https://$host/oauth2/auth"
    nginx.ingress.kubernetes.io/auth-signin: "https://$host/oauth2/start?rd=$escaped_request_uri"
  name: external-auth-oauth2
  namespace: staging
spec:
  rules:
  - host: test3.sdp.handlel.app
    http:
      paths:
      - backend:
          serviceName: kubernetes-dashboard
          servicePort: 80
        path: /
  tls:
  - hosts:
    - test3.sdp.handlel.app
    secretName: test3-sdp-handlel-app-tls
---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: oauth2-proxy
  namespace: staging
spec:
  rules:
  - host: test3.sdp.handlel.app
    http:
      paths:
      - backend:
          serviceName: oauth2-proxy-service
          servicePort: 4180
        path: /oauth2
  tls:
  - hosts:
    - test3.sdp.handlel.app
    secretName: test3-sdp-handlel-app-tls