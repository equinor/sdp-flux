---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: aware
    tier: api
  name: aware-api
  namespace: monitoring
  annotations:
    flux.weave.works/automated: 'true'
    flux.weave.works/tag.aware-api: semver:~3.*
spec:
  replicas: 1
  selector:
    matchLabels:
      app: aware
      tier: api
  template:
    metadata:
      labels:
        app: aware
        tier: api
    spec:
      containers:
      - name: aware-api
        image: sdpequinor/aware-api:3.1.8
        imagePullPolicy: Always
        ports:
        - containerPort: 5000
          name: http
        env:
        - name: PROMETHEUS_API
          value: "http://prometheus-operator-prometheus.monitoring:9090/api/v1/alerts"
        - name: DEPLOYMENT_NAME
          value: "SDP-AKS Dev"
        - name: IGNORE_PROMETHEUS_ALERTS
          value: "CoreDNSDown,KubeAPIDown,KubeControllerManagerDown,KubeSchedulerDown,KubeletDown,TargetDown,KubeAPIErrorsHigh,Watchdog"
        - name: EXPORT_SECRET
          valueFrom:
            secretKeyRef:
              name: aware-export-secret
              key: EXPORT_SECRET
        resources:
          requests:
            cpu: 100m
            memory: "100Mi"
          limits:
            cpu: 500m
            memory: "1Gi"
